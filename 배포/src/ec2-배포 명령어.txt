C:\workspaces\fastAPI\src\my_memo_app_mvc_docker 위치에서 여기에 powershell창 열기
ps> =>  powershell
ec2> => aws
 
ps>ssh -i "xxx.pem" ec2-user@xxx.xxx.xxx.xxx  # 반드시 xxx.pem 파일이 위치한 폴더에서 실행
ps>ssh -i "miniproj-fastAPI.pem" ec2-user@13.54.44.223
★★★★★ C:\workspaces\fastAPI\src\my_memo_app_mvc_docker 폴더에서 삭제 => C:\Users\admin\Desktop\AWS관련 폴더에 miniproj-fastAPI.pem 백업

-접속 거부시
1. 상속 권한 제거
icacls .\xxx.pem /inheritance:r
ps>icacls .\miniproj-fastAPI.pem /inheritance:r

2. 현재 사용자에게만 권한 부여
icacls .\xxx.pem /grant:r "$($env:USERNAME):(R)"
ps>icacls .\miniproj-fastAPI.pem /grant:r "$($env:USERNAME):(R)"

3. 권한 확인
icacls .\xxx.pem
ps>icacls .\miniproj-fastAPI.pem

4. 다시 접속 확인
ssh -i ".\xxx.pem" ec2-user@xxx.xxx.xxx.xxx
ps>ssh -i ".\miniproj-fastAPI.pem" ec2-user@13.54.44.223

- docker 설치
ec2>sudo dnf update -y
ec2>sudo dnf install docker -y
ec2>sudo systemctl enable --now docker
ec2>sudo systemctl start docker
ec2>sudo usermod -aG docker ec2-user
ec2>exit
ec2>ssh -i ".\miniproj-fastAPI.pem" ec2-user@13.54.44.223  재접속
ec2>docker --version   # 재접속 후 확인

- compose v2 설치
ec2>mkdir -p ~/.docker/cli-plugins
ec2>curl -SL \
         https://github.com/docker/compose/releases/latest/download/docker-compose-linux-x86_64 \
         -o ~/.docker/cli-plugins/docker-compose

ec2>chmod +x ~/.docker/cli-plugins/docker-compose
ec2>docker compose version

GHCR(Git Hub Container Register)
gitHub access를 위한 토큰 발급 (중간 저장소 역할)  
Settings - Developer Settings token(classic) 발급

Token 키: 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    2026.09.14)

- GHCR: 로컬(vs code 터미널)에서 ..\my_memo_app_mvc_docker\backend 폴더로 이동
ps>echo "GHCR_token" | docker login ghcr.io -u [깃허브계정] --password-stdin
ps>echo "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" | docker login ghcr.io -u yjnam7229 --password-stdin
   ===> Login Succeeded 메시지 확인

- GHCR을 이용한 도커 컨테이너 배포하기(깃허브 컨테이너 레지스트리에 배포)
ps>docker build --platform linux/amd64 -t ghcr.io/yjnam7229/my-memo-app:1.0.0  .
ps>docker push ghcr.io/yjnam7229/my-memo-app:1.0.0
   ===> github의 packages탭의 my-memo-app 확인 가능

- ec2 인스턴스에 접속해서 역시 docker login 명령으로 깃허브 레지스트리에 접속-이미지 다운받기
ec2>echo "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" | docker login ghcr.io -u yjnam7229 --password-stdin       ===> Login Succeeded 메시지 확인
ec2>docker pull ghcr.io/yjnam7229/my-memo-app:1.0.0

- EC2 인스턴스에 사용할 수 있도록 docker-compose.yml 수정 
   ==> C:\workspaces\fastAPI\src\my_memo_app_mvc_docker\ec2-docker-compose.yml 파일에서 image: ghcr.io/yjnam7229/my-memo-app:1.0.0 변경

ec2>mkdir -p ~/my_memo_app_mvc_docker
ec2>cd ~/my_memo_app_mvc_docker                     ==> [ec2-user@ip-172-31-33-82 my_memo_app_mvc_docker]$
ec2>nano docker-compose.yml   # ctl+o/enter/ctl+x   편집 모드에서 순서대로 진행
ec2>cat docker-compose.yml

ec2>docker compose up -d   # 컴포즈 실행하여 배포
[ec2-user@ip-172-31-33-82 my_memo_app_mvc_docker]$docker compose up -d

접속 확인 
http://13.54.44.223:8000/

MySQL 컨테이너 내부로 접속하여 접근하기
[ec2-user@ip-172-31-33-82 my_memo_app_mvc_docker]$docker exec -it myproject-db mysql -u root -p

mysql> SHOW DATABASES;

사용할 데이터베이스 선택하기
USE my_memo_app;

테이블 조회하기
SELECT *FROM my_memo_app.memo;

한글 깨짐 명령어
SET NAMES utf8mb4;





